"use strict";
var fluid = require("infusion")

/*
        // Should only relay model changes to the query.
        modelToState:       false,
        modelToQuery:       true,
        queryToModel:       false,

        // Should only relay model changes to the state.
        modelToState:       true,
        modelToQuery:       false,
        queryToModel:       false,

        // Should only relay URL changes to the the model.
        modelToState:       false,
        modelToQuery:       false,
        queryToModel:       true,

 */
